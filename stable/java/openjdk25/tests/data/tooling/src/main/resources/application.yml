# Rule: openjdk21-to-openjdk25+-00040
# Build configuration referencing jdeps with removed -profile option
build:
  steps:
    - name: analyze-dependencies
      command: jdeps -profile lib/myapp.jar
