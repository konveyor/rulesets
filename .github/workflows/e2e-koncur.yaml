name: E2E Koncur

on:
  push:
    branches:
    - main
    - 'release-*'
  pull_request:
    branches:
    - main
    - 'release-*'
  workflow_dispatch:

jobs:
  e2e-koncur:
    if: github.repository == 'konveyor/rulesets'
    # TODO: switch to konveyor/ci/.github/workflows/e2e-koncur.yaml@main once
    # konveyor/ci#272 merges. Points at the fork branch for now so this PR can
    # exercise the combined hub + kantra koncur workflow end to end.
    uses: mguetta1/ci/.github/workflows/e2e-koncur.yaml@support-rulesets-e2e-hub
